<%= form_for(@favorite) do |f| %>
  <% if @favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% @favorite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Colectivo" %><br />
    <%= f.select :bus_id, options_from_collection_for_select(Bus.all, :id, :name) %>  
  </div>
  <div class="field">
    <%= f.label "Parada" %><br />
    <%= f.text_field :stop %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
