<div class="page-header">
<h1>Llegada.</small></h1>
</div>

<div class="row">
  <div class="span12">
  <h3>Colectivo tal</h3>
  <p><%= @llegada %></p>
  </div>

  <div class="span4">
    <% if user_signed_in? %>
      <% unless favorite_is_present%>
      <%= link_to "Agregar a favoritos", Favorite.create(:bus_id => Bus.find_by_value(params[:line]), :stop => params[:stop], :user => current_user) %>
      <% end %>
    <% end %>
    <%= link_to "Volver", :back %>
  </div>

</div>